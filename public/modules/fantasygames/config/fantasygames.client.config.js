'use strict';

// Configuring the Draft module
/*angular.module('draft').run(['Menus',
	function(Menus) {
		// Set top bar menu items
		Menus.addMenuItem('topbar', 'Drafts', 'drafts', 'dropdown', '/drafts(/join)?');
	}
]);*/ 
